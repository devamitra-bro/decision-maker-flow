[pytest]
asyncio_mode = auto
markers =
    integration_postgres: marks tests as requiring a real postgres testcontainer (deselect with -m "not integration_postgres")
    requires_gateway: marks tests as requiring a running kind cluster (deselect with -m "not requires_gateway")
    unit: marks tests as pure unit tests with no network or external dependencies
